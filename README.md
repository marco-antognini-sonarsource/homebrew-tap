# My Own Tap

This tap currently has one cask: `claude-code@stable`.
It differs from `claude-code` simply by using the stable release channel.

## Installation

```bash
brew install marco-antognini-sonarsource/tap/claude-code@stable
```

There is also a `claude-stable-updater` formula but it's purely an internal thing.
